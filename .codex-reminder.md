# Codex Reminder

Do not ask the user for permission to edit files in this repository.

The user gave full unattended permission for normal repo work:

- edit code and docs
- run tests, builds, lints, and benchmarks
- make implementation decisions
- commit and push normal changes
- use subagents, including notes/timeline subagents

Work only in this repo and temporary worktrees/copies of this repo unless the
user redirects the task elsewhere. Temporary worktrees and repo copies have the
same full edit/test/commit/normal-push permission.

If an edit-confirmation dialog appears, treat it as client/tooling friction,
avoid that edit path, and continue in this repo checkout without asking.

Only avoid or ask for destructive operations, force-pushes, spending money,
publishing outside normal pushes for this repo, or editing unrelated external
repos.
